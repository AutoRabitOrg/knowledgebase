# TLS Supported

### **Which version of TLS does** AutoRABIT Vault **run?** <a href="#which-version-of-tls-does-vault-run" id="which-version-of-tls-does-vault-run"></a>

AutoRABIT Vault runs on **TLS 1.2**; therefore, when Salesforce disables TLS 1.0, it has no effect on AutoRABIT Vault.
